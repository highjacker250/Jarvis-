<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/jarvis_bg">

    <!-- TOP BAR -->
    <LinearLayout
        android:id="@+id/top_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="12dp"
        android:paddingTop="48dp"
        android:paddingBottom="12dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="JARVIS AI"
            android:textColor="@color/jarvis_cyan"
            android:textSize="22sp"
            android:letterSpacing="0.15"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/tv_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="READY"
            android:textColor="@color/jarvis_green"
            android:textSize="11sp"
            android:textStyle="bold"
            android:letterSpacing="0.1"
            android:layout_marginEnd="8dp"/>

        <ImageButton
            android:id="@+id/btn_history"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@android:drawable/ic_menu_recent_history"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:tint="@color/jarvis_cyan"
            android:contentDescription="History"/>

        <ImageButton
            android:id="@+id/btn_settings"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@android:drawable/ic_menu_preferences"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:tint="@color/jarvis_cyan"
            android:contentDescription="Settings"/>
    </LinearLayout>

    <!-- JARVIS ORB (Lottie or fallback) -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottie_orb"
        android:layout_width="220dp"
        android:layout_height="220dp"
        app:lottie_rawRes="@raw/ai_orb"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:layout_constraintTop_toBottomOf="@id/top_bar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/card_response"
        android:layout_marginTop="8dp"/>

    <!-- RESPONSE CARD -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_response"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        app:cardBackgroundColor="@color/jarvis_dark_card"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toTopOf="@id/tv_transcript"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tv_response"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="80dp"
            android:gravity="center"
            android:text="Namaskar! Main Jarvis hoon.\nMic tap karein ya \'Hey Jarvis\' bolein."
            android:textColor="@color/jarvis_white"
            android:textSize="16sp"
            android:lineSpacingExtra="4dp"
            android:padding="20dp"/>
    </androidx.cardview.widget.CardView>

    <!-- TRANSCRIPT TEXT -->
    <TextView
        android:id="@+id/tv_transcript"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mic tap karein ya \'Hey Jarvis\' bolein"
        android:textColor="@color/jarvis_white_60"
        android:textSize="13sp"
        android:gravity="center"
        android:paddingTop="12dp"
        android:paddingBottom="8dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        app:layout_constraintBottom_toTopOf="@id/btn_mic"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- MIC BUTTON -->
    <ImageButton
        android:id="@+id/btn_mic"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@android:drawable/ic_btn_speak_now"
        android:background="@drawable/mic_button_bg"
        android:tint="@color/black"
        android:contentDescription="Mic button"
        android:layout_marginBottom="48dp"
        android:scaleType="centerInside"
        android:padding="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
